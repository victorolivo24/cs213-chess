package chess;

/**
 * TODO: Implement the board container.
 * <p>
 * Board will store the 8x8 grid of {@link Piece} instances, provide mutators used by Game when
 * applying moves, and offer a read-only {@link chess.Piece.PositionView} implementation so pieces can
 * generate moves. It will also expose snapshot/restore utilities for simulating moves safely.
 */
public class Board {
    // Fields and logic will be added alongside the Game implementation.
}
