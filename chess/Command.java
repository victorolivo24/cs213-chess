package chess;

/**
 * TODO: Define the sealed hierarchy representing parsed player input.
 * <p>
 * Expect subclasses for normal piece moves, resignations, and draw offers per the assignment spec.
 * MoveParser will emit these, and Game will interpret them.
 */
public class Command {
    // Placeholder until MoveParser and Game are implemented.
}
